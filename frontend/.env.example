# 로컬에서 Vite만 실행할 때 API/OAuth 요청을 9000번 Dahum 게이트웨이로 전달합니다.
VITE_DEV_API_TARGET=http://localhost:9000

# 운영 빌드는 React와 Spring이 같은 origin이므로 비워 둡니다.
# 프론트와 API를 서로 다른 origin으로 개발할 때만 지정하세요.
VITE_API_BASE_URL=
